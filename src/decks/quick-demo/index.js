// Import deck configuration
import deckConfig from './deck.json';

// Quick Demo uses hardcoded HTML slides, so no MDX modules or jokes needed
export const mdxModules = {};
export const config = deckConfig;
export const jokes = null;

export default {
  config,
  mdxModules,
  jokes,
};
